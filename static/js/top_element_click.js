$(".top_element_click").on('click', function(event){
	var productId = $(this).attr('class');
    //alert(productId);
    // var element = this;
    // alert(this.nextSibling)
    // alert($(element).siblings().innerHTML);
    // var div= this.nextSibling;
    // div.style.backgroundColor = "red";
    // div.style.maxHeight = mHeight;
    // div.style.height= auto;
    // //alert(x);
    // alert(x.innerHTML)
    // x.style.maxHeight = "1000px";
    // event.stopPropagation();
    // event.stopImmediatePropagation();
    //(... rest of your JS code)
});